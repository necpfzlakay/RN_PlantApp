


import { width, fontScale, height, scale } from "./dimensions";
import { colors } from "./colors";
import { font_size, fonts_family } from "./fontsize";
import { NavigationScreens } from "./navigationScreens";

const calculatedPaddingHorizontal = width * 24 / 375


export {
  width,
  fontScale,
  height,
  scale,
  colors,
  font_size,
  fonts_family,
  NavigationScreens,
  calculatedPaddingHorizontal
}